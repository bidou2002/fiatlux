# Research experiments

This directory preserves research and optical-bench scripts for reference. They are
not part of the supported Fiatlux API and are not guaranteed to run without adaptation.
Some require local FITS datasets or hardware-specific configuration; paths and
parameters must be supplied for the user's environment before execution.

Reusable demonstrations should be placed in `examples/`, while progressive learning
material belongs in `tutorials/`.
