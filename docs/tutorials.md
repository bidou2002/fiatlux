# Tutorial environment and validation

The maintained notebooks in `tutorials/` support Python 3.10 through 3.12 and
the dependency set declared by the `tutorials` extra:

```bash
python -m pip install -e '.[tutorials]'
```

Notebooks 00–05 and 07–09 are self-contained and must execute independently from
a clean kernel. Notebook 07 demonstrates a complete ZELDA closed loop, from
interaction-matrix calibration to the converged residual OPD. Continuous
integration executes each notebook in a separate kernel, so variables and
outputs from another tutorial cannot mask a missing definition.

Notebook 06 requires a directory of HARMONI residual FITS files. Set its path
before launching Jupyter:

```bash
export FIATLUX_HARMONI_DATA=/path/to/harmoni_residuals
jupyter lab
```

If the variable is absent, the notebook stops immediately with an actionable
message. CI generates a small synthetic dataset matching the documented FITS
contract and executes notebook 06 against it.

Stored cell outputs and execution counters are deliberately cleared from the
repository. Figures are regenerated by running the notebooks; this keeps the
Git history compact and ensures displayed results never conceal API drift.
